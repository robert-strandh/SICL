Write a structured version of the lambda lists of each CL function.

Exploit this in Climacs, Climed.

Make this functionality available to other packages as well.

There should probably be both a description of the type as a Lisp
expression and in English (internationalization).  Or perhaps write an
interpreter for type specifyers to various languages. 

The compiler could exploit this information as well.